{
  "Ars": {
    "ServiceHost": "ars-host.example.local",
    "BindUser": "EXAMPLE\\bind-account",
    "BindPassword": "REPLACE_ME",

    "_comment_fastread": "Phase 2 fast read (read/bench default to it). AdHost = a DC for raw LDAP; ArsSqlConnString = read-only conn to the ARS config DB for CVSAValues/VirtualSchema. NEVER commit a populated copy.",
    "AdHost": "dc01.example.local",
    "ArsSqlConnString": "Server=sqlhost;Database=ActiveRoles830;User Id=READONLY;Password=REPLACE_ME;Encrypt=True;TrustServerCertificate=True"
  }
}
