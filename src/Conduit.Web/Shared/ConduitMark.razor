@*
    ConduitMark — deprecated shim. Re-exports CenturyCity.Design.Components.Marks.CcConduitMark.
    Kept so existing callers compile; new pages should use <CcConduitMark> directly.
    Delete after all callers migrate (search the repo for "<ConduitMark").
*@
@inherits CcConduitMark
