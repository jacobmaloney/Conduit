namespace Conduit.Sync;

/// <summary>
/// Phase 0 placeholder. The Conduit.Sync project exists to establish the
/// namespace, project boundary, and project references. Sync engine contracts
/// (IConnector, ISyncEngine, IConnectorRegistry, SyncProject model) are
/// designed and added in Phase 1 once the protocol and data-ownership
/// decisions in PLANNING-FOR-COWORK.md §9.3 are locked.
///
/// Do not extend this file with speculative interfaces.
/// </summary>
internal static class PlaceholderMarker { }
