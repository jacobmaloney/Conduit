@*
    CenturyCityMark — deprecated shim. Re-exports CenturyCity.Design.Components.Marks.CcCenturyCityMark.
    Kept so existing callers compile; new pages should use <CcCenturyCityMark> directly.
    Delete after all callers migrate (search the repo for "<CenturyCityMark").
*@
@inherits CcCenturyCityMark
